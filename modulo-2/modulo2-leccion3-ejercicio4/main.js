"use strict";
let access = document.querySelector(".dog__age");
const YearUno = 1;
const YearTwo = 2;

if access.innerHTML == YearUno) {
  access.innerHTML = "15";
} else if (access.innerHTML === YearTwo {
  access.innerHTML = 24;
} else (access.innerHTML >= 3) {
  access.innerHTML = 24 + parseInt.access.innerHTML * 5;
}
